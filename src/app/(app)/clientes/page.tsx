import { PagePlaceholder } from "@/components/app-shell/page-placeholder";

export default function ClientesPage() {
  return (
    <PagePlaceholder
      title="Clientes"
      subtitle="Clientes e entregas ao sistema jurídico"
      phase="A8"
      description="A conversão de oportunidade ganha em cliente, com o registro de handoff, entra depois do pipeline."
    />
  );
}
