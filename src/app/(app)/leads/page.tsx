import { PagePlaceholder } from "@/components/app-shell/page-placeholder";

export default function LeadsPage() {
  return (
    <PagePlaceholder
      title="Leads"
      subtitle="Lista de leads do escritório"
      phase="A4"
      description="A lista de leads chega depois de contatos, identidade e deduplicação (A3), que são a base de qualquer registro de pessoa no sistema."
    />
  );
}
