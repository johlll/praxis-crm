import { PagePlaceholder } from "@/components/app-shell/page-placeholder";

export default function ConfiguracoesPage() {
  return (
    <PagePlaceholder
      title="Configurações"
      subtitle="Equipe, pipelines, integrações e auditoria"
      phase="A2"
      description="As configurações começam pela equipe e pelos convites, junto da autenticação."
    />
  );
}
