import { PagePlaceholder } from "@/components/app-shell/page-placeholder";

export default function AutomacoesPage() {
  return (
    <PagePlaceholder
      title="Automações"
      subtitle="Regras automáticas do CRM"
      phase="C1"
      description="As automações são do marco C, com modo simulação obrigatório antes de qualquer regra que envie mensagem."
    />
  );
}
