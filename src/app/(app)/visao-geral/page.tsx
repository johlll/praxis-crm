import { PagePlaceholder } from "@/components/app-shell/page-placeholder";

export default function VisaoGeralPage() {
  return (
    <PagePlaceholder
      title="Visão geral"
      subtitle="Painel executivo do escritório"
      phase="A10"
      description="O dashboard será ligado a consultas reais sobre os dados do banco. Nenhum número aparece aqui antes de existir uma consulta que o produza."
    />
  );
}
